import { Metadata } from "next";


export const metadata:Metadata = {
    title: "Friends"
}

const Page = () => {
    return (
        <div>
            Friends Page
        </div>
    );
};

export default Page;